#include "lists.h"
#include <stdio.h>


/**
 * check_cycle - will check if there is a cycle in a linked list
 * @list: the linked list to be checked
 *
 * Return: 0 if there's no cycle, 1 if there is
 */
int check_cycle(listint_t *list)
{
    listint_t *turtle = list;

    free(turtle);
    return (0);
}
